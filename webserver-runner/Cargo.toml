[package]
name = "webserver-runner"
edition.workspace = true
rust-version.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
api-server = { path = "../webserver-lib" }

clap = { workspace = true, features = ["derive"] }
tokio = { workspace = true }
